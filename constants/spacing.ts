/**
 * Spacing — base scale 8 / 16 / 24 / 32.
 * Card list rhythm: padding inside cards, gap between cards, gap between sections.
 */
export const Spacing = {
  xs: 8,
  sm: 16,
  md: 24,
  lg: 32,
} as const;

export const SCREEN_PADDING = 16;

/** Padding inside cards (list items, panels). */
export const CARD_PADDING = 16;

/** Vertical/list gap between adjacent cards. */
export const CARD_GAP = 12;

/** Gap between major sections on a screen. */
export const SECTION_GAP = 24;

export const BUTTON_HEIGHT = 48;

export const BUTTON_RADIUS = 24;

export const CARD_RADIUS = 12;
